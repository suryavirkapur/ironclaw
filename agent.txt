current context:
- implemented guest browser tooling with headless chromium/chrome execution in
  `crates/tools/src/browser.rs`
- browser tool supports `fetch`, `screenshot`, and `evaluate` actions with input parsing from
  json or compact command strings
- added browser safety controls: allowlisted domains, dangerous scheme blocking, timeout
  enforcement, and linux process resource limits
- wired browser registration in guest runtime and host tool allowlist exposure for guest sessions
- updated guest rootfs builder to install chromium when available and warn gracefully when absent

pending tasks:
- run full cargo test coverage for browser integration once crates.io/network access is available
- validate browser tool behavior inside firecracker vm with a chromium-enabled rootfs image
- decide whether to add a dedicated host config gate for enabling/disabling browser tool exposure

important decisions:
- kept browser as a single guest tool (`browser`) with action-dispatch input
  instead of separate tool names to minimize protocol churn
- defaulted browser allowlist to empty so all domains are denied until explicitly configured
- enforced browser sandbox controls in-process via linux `setrlimit` + `prctl(no_new_privs)`
  without adding extra guest daemons

architectural notes:
- guest browser config is now part of `common::config::GuestConfig` under `[browser]`
- host runtime now includes `browser` in tool allowlists sent to guest runtimes
- rootfs builder installs `/usr/bin/chromium` from host `chromium/chromium-browser/google-chrome`
  binaries when present

plan:
- run vm smoke tests for browser fetch/screenshot using allowlisted domains
- add evaluate integration tests against real chromium once network + package cache is available
