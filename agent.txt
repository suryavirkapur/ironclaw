current context:
- expanded ironclaw to v0.10.0 with channel gateway, heartbeat, and admin api foundations
- removed thiserror and anyhow from all crates; all errors use hand-written impls
- added `crates/channels` for telegram + whatsapp cloud api webhook parsing
- added `crates/heartbeat` for tokio-based periodic scheduler (30-min default interval)
- added `crates/ironclawd/src/api.rs` with utoipa-annotated openapi routing module
- scalar api docs ui served at `/api/docs`
- added tokio-postgres + deadpool-postgres + askama + askama_web deps to ironclawd
- new api routes merged into existing axum app via `Router::merge`

pending tasks:
- wire channel webhook handlers to actually route inbound messages to guest via vsock/ipc
- implement postgres connection pool in appstate and wire admin endpoints
- implement postgres memory schema (memories, context_windows tables)
- build solid.js admin panel ui under `ui/` with vm monitoring, key management, memory view
- implement heartbeat scheduler startup in ironclawd main server loop
- add askama templates for server-rendered admin views
- create dockerfile for single-binary build with embedded ui assets
- implement skill sandboxing: parse third-party skill.md strictly on guest side only

important decisions:
- kept existing legacy routes (ws, gateway, webhooks, soul-guard) intact; new api module
  adds routes alongside via router merge rather than replacing
- chose utoipa 5 + scalar for openapi; spec auto-generated from annotated handlers
- chose askama 0.15 + askama_web 0.15 for server-rendered templates (axum-0.8 feature)
- chose tokio-postgres + deadpool (no orm) for host-side postgres
- all error types are now manual impls — no thiserror or anyhow anywhere in workspace

architectural notes:
- api module (`api.rs`) owns all new endpoints; legacy routes stay in `main.rs`
- channel crate parses raw webhook payloads into `InboundMessage` structs
- heartbeat crate is standalone; ironclawd owns the callback wiring
- host-side api keys stored in postgres, never exposed to guest runtime
- memory entries stored on host in postgres; context window injected into guest per execution
- whatsapp cloud api uses get verification + post webhook pattern
