id = "qa-engineer"
name = "Zoe"
role = "QA Engineer"
reports_to = "engineering-lead"

[model]
provider = "openrouter"
model = "minimax/minimax-m2.5"

[memory]
engine = "core-agent-memory"
private = true
collections = ["quality", "regressions", "release-evidence"]

[wasm]
tools_dir = "tools"
may_create = true
may_publish = false

[a2a]
accept_from = ["engineering-lead", "backend-engineer", "frontend-engineer"]
max_delegation_depth = 1
max_concurrent_tasks = 4

[[skills]]
id = "verify_release"
description = "Design and execute release verification, then report pass/fail evidence and residual risk."
